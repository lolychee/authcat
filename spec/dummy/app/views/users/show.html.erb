<div class="main-page">

    <table class="pure-table">
      <thead>
        <tr><th colspan="2">User #<%= @user.id %></th></tr>
      </thead>
      <% @user.attributes.each do |key, value| %>
        <tr><td><%= key %></td><td><%= value %></td></tr>
      <% end %>
    </table>

</div>
